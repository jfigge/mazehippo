The Flutter package lives here. Everything worth reading is a directory up:
`../README.md` for what the game is and why it works the way it does, and
`../CLAUDE.md` for the layout, the commands and the traps.
